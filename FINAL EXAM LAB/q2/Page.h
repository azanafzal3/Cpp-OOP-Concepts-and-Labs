#ifndef PAGE_H
#define PAGE_H

class Page
{
	public:
		Page();
		Page (operator &page);
		Page::Page(operator +=[2][2]);
		Page::Page(opertor +=l);
		Page::Page(operator =x);
		Page::Page(operator []); 
		~Page();
	protected:
};

#endif